{
  "permissions": {
    "allow": [
      "Bash(npm install *)",
      "Bash(npm run *)",
      "Bash(node -p \"process.arch + ' ' + process.version\")",
      "Bash(grep -q \"^\\\\.env\\\\.local$\\\\|^\\\\.env\\\\*\" .gitignore)"
    ]
  }
}
